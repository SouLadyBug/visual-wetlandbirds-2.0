# Configuration

`config.yaml` contains paths and experiment settings. Paths are relative to the repository root unless changed by the user.

The default training values are intentionally conservative. They reproduce the scale of the supplied scripts rather than claiming to be an optimized training recipe.
